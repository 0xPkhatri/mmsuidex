export async function runMarketMaker(spread: number) {
  // Placeholder for real Sui order-book logic
  console.log("Market maker started with spread:", spread);

  /*
      Future logic:
      - Fetch mid-price from order book
      - Place bid/ask limit orders
      - Monitor fills
      - Rebalance inventory
    */
}
